apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: notes-app
spec:
  selector:
    app: notes-app
  ports:
    - port: 5173
      targetPort: 5173
      nodePort: 30517
  type: NodePort
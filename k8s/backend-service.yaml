apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: notes-app
spec:
  selector:
    app: notes-app
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30800
  type: NodePort